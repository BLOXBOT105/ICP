#include "Logging.h"

int CheckLogExistence() {
    int file_exists = 0;
    
    // needs implemented
    if(file_exists == 0) { return 0; }

    return 1;
}

int __overwrite_log__() {
    return 1;
}

void WriteToLog(string log) {
    
}